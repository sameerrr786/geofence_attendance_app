name: geofence_attendance_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  geolocator: ^14.0.2
  geodesy: ^0.10.2
  camera: ^0.11.2+1
  
  # --- Face Detection (These are working) ---
  google_ml_kit: ^0.20.0
  google_mlkit_face_detection: ^0.13.1
  google_mlkit_commons: ^0.11.0
  
  supabase_flutter: ^2.10.3
  image_picker: ^1.2.0
  intl: ^0.20.2

  # --- Face Recognition (Re-adding these) ---
  tflite_flutter: ^0.12.1
  image: ^4.1.7
  http: ^1.2.1
  collection: ^1.18.0
  path_provider: ^2.0.0 # Needed for image cropping

# --- This section is ESSENTIAL to fix the build error ---
dependency_overrides:
  collection: ^1.19.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  
  # --- Make sure your model file is here ---
  assets:
    - assets/mobilefacenet.tflite

